import Vue from 'vue'
import Vuex from 'vuex'

import crud from './helpers/crud'
import apicomm from './modules/apicomm'
/* -- vuexcrud inject imports -- do not modify this comment */

Vue.use(Vuex);

const debug = process.env.NODE_ENV !== 'production';

export default new Vuex.Store({
	modules: {
		apicomm,
	    /* -- vuexcrud inject module -- do not modify this comment */
	},
	strict: debug,
	plugins: debug ? [] : [],
	state: {
	},
	getters: {
	},
	mutations: {
	}
})