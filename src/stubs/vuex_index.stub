import Vue from 'vue'
import Vuex from 'vuex'

import crud from './helpers/crud'
import apicomm from './modules/apicomm'

Vue.use(Vuex);

const debug = process.env.NODE_ENV !== 'production';

export default new Vuex.Store({
	modules: {
		apicomm
	},
	strict: debug,
	plugins: debug ? [] : [],
	state: {
	},
	getters: {
	},
	mutations: {
	}
})